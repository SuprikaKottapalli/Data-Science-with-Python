import Numpy as np
  #np is alias name foe Numpy we can give whatever we want
a = np.array([9,8,7])
b = np.array([[9,8,7],[1,2,3]])

# a.ndim -- Dimension of array
# a.size -- no of elements ins array
# a.dtype -- data type
# a.shape -- dimensions
# itemsize -- each item size
# a.nbytes -- total bytes

print(a.size)
